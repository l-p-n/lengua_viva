<h2>Comments</h2>
<% if resource.comments.count > 0 %>
  <% resource.comments.each do |comment| %>
    <p><%= comment.content %></p>
    <p>Author: <%= comment.user.first_name %></p>
    <p><%= comment.created_at %></p>
  <% end %>
<% else %>
  <p>No comments yet.</p>
<% end %>
